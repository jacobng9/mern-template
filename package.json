{
  "devDependencies": {
    "@types/node": "^22.10.3"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npm install && npm install --prefix client && npm run build --prefix client",
    "start": "cd server && node --env-file=config.env server"
  }
}
